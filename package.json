{
  "name": "logic-bugs-web",
  "version": "0.1.0",
  "description": "Jogo de puzzle 2D em grade inspirado nos princípios do Berusky — versão Web educativa.",
  "private": true,
  "license": "GPL-2.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.7.0"
  }
}
